<!DOCTYPE html>
<html lang="en">
<head>
    
    <script async src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-2225138889895982"
    crossorigin="anonymous"></script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> Admin Sign In</title>
    <style>
        body {
            font-family: 'Courier New', Courier, monospace; /* Monospace font for hacker vibe */
            margin: 0;
            padding: 0;
            background-color: #000; /* Dark background color */
            color: #0f0; /* Green text color */
        }

        .container {
            width: 80%;
            max-width: 400px;
            margin: 100px auto;
            padding: 30px 20px; /* Increased padding */
            background-color: #111; /* Dark background color */
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 255, 0, 0.1); /* Green shadow */
        }

        h2 {
            text-align: center;
            color: #0f0; /* Green text color */
            margin-bottom: 30px;
        }

        input[type="text"],
        input[type="password"] {
            width: 100%;
            padding: 15px; /* Increased padding */
            margin-bottom: 20px;
            border: 1px solid #0f0; /* Green border */
            border-radius: 5px;
            box-sizing: border-box;
            font-size: 16px;
            background-color: #000; /* Dark background color */
            color: #0f0; /* Green text color */
        }

        input[type="submit"] {
            width: 100%;
            padding: 15px; /* Increased padding */
            background-color: #0f0; /* Green button background color */
            color: #000; /* Black text color */
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 16px;
            transition: background-color 0.3s ease;
        }

        input[type="submit"]:hover {
            background-color: #007700; /* Dark green on hover */
        }

        .message {
            text-align: center;
            color: #f00; /* Red message color */
            margin-top: 20px;
        }

        @media screen and (max-width: 480px) {
            .container {
                margin: 50px auto;
                padding: 20px; /* Adjusted padding */
            }
        }
        
        /* CSS for role option */
        #role {
            width: 100%;
            padding: 10px;
            border-radius: 5px;
            border: 1px solid #0f0; /* Green border */
            box-sizing: border-box;
            font-size: 16px;
            appearance: none; /* Remove default arrow */
            -webkit-appearance: none; /* Remove default arrow for WebKit browsers */
            -moz-appearance: none; /* Remove default arrow for Mozilla browsers */
            background-color: #000; /* Dark background color */
            color: #0f0; /* Green text color */
            cursor: pointer;
            margin-bottom: 20px;
        }

        #role option {
            background-color: #000; /* Dark background color */
            color: #0f0; /* Green text color */
        }

        #role:focus {
            outline: none; /* Remove focus outline */
            border-color: #0f0; /* Green border color on focus */
        }
    </style>
</head>
<body>
  
    
    <div class="container">
        <h2> Admin Pannel</h2>
        <form action="/adminsinIn" method="post">
            <input type="text" name="email" placeholder="Email" required>
            <div class="form-group">
                <label for="role"> Select your Role</label>
                <select id="role" name="role" required>
                    <option value="Admin">As Admin </option>
                    <option value="Other">As Student</option>
                </select>
            </div>
            <input type="password" name="password" placeholder="VPI  Password" required>
            <input type="submit" value="Sign In">
        </form>
    </div>
</body>
</html>
