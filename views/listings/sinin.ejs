<!DOCTYPE html>
<html lang="en">
<head>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" integrity="sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <script async src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-2225138889895982"
    crossorigin="anonymous"></script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign In</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4;
        }

        .container {
            width: 80%;
            max-width: 400px;
            margin: 100px auto;
            padding: 30px 20px; /* Increased padding */
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        h2 {
            text-align: center;
            color: #333;
            margin-bottom: 30px;
        }

        input[type="text"],
        input[type="password"] {
            width: 100%;
            padding: 15px; /* Increased padding */
            margin-bottom: 20px;
            border: 1px solid #ccc;
            border-radius: 5px;
            box-sizing: border-box;
            font-size: 16px;
        }

        input[type="submit"] {
            width: 100%;
            padding: 15px; /* Increased padding */
            background-color: #007bff;
            color: #fff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 16px;
            transition: background-color 0.3s ease;
        }

        input[type="submit"]:hover {
            background-color: #0056b3;
        }

        .message {
            text-align: center;
            color: #b71a97;
            margin-top: 20px;
        }

        @media screen and (max-width: 480px) {
            .container {
                margin: 50px auto;
                padding: 20px; /* Adjusted padding */
            }
        }
              /* CSS for role option */
#role {
    width: 100%;
    padding: 10px;
    border-radius: 5px;
    border: 1px solid #ccc;
    box-sizing: border-box;
    font-size: 16px;
    appearance: none; /* Remove default arrow */
    -webkit-appearance: none; /* Remove default arrow for WebKit browsers */
    -moz-appearance: none; /* Remove default arrow for Mozilla browsers */
    background-color: #fff; /* Background color */
    color: #333; /* Text color */
    cursor: pointer;
    margin-bottom: 20px;
}

#role option {
    background-color: #fff; /* Background color */
    color: #333; /* Text color */
}

#role:focus {
    outline: none; /* Remove focus outline */
    border-color: #007bff; /* Border color on focus */
}

/* New CSS for password visibility toggle */
.password-toggle {
    position: absolute;
    top: 50%;
    right: 10px;
    transform: translateY(-50%);
    cursor: pointer;
}
.message-container a {
            display: inline-block;
            margin: 10px;
            padding: 10px 20px;
            background-color: #dddd16;
            color: #fff;
            text-decoration: none;
            border-radius: 5px;
            transition: background-color 0.3s ease;
        }

        .message-container a:hover {
            background-color: #0056b3;
        }
        .reset{
            color: rgb(226, 54, 19) !important;
        }
        .social-login-buttons {
            margin-top: 20px;
        }

        .social-login-buttons a {
            display: inline-block;
            margin: 5px;
            padding: 10px;
            border-radius: 5px;
            background-color: #3b5998;
            color: #fff;
            text-decoration: none;
            font-size: 20px;
        }

        .social-login-buttons a:hover {
            background-color: #314c85;
        }

        .social-login-buttons {
            text-align: center;
        }

        .social-login-buttons a {
            display: inline-block;
            margin: 10px;
            padding: 10px;
            border-radius: 50%;
            background-color: #ccc;
        }

        .social-login-buttons a i {
            font-size: 24px;
            color: #fff;
        }

        .social-login-buttons a.google i {
            color: #DB4437 ; /* Google Red */
        }

        .social-login-buttons a.facebook i {
            color: #3B5998; /* Facebook Blue */
        }

        .social-login-buttons a.github i {
            color: #211F1F; /* GitHub Black */
        }


    </style>
</head>
<body>
  
    
    <div class="container">
        <h2>Sign In</h2>
        <form action="/sinIn" method="post">
            <input type="text" name="email" placeholder="Email" required>
            <div class="form-group">
                <label for="role"> Select your Role</label>
                <select id="role" name="role" required>
                  
                    <option value="donor">As Donor</option>
                    <option value="student">As Student</option>
                </select>
            </div>
            <div style="position: relative;">
                <input type="password" id="password" name="password" placeholder="Password" required>
                <span class="password-toggle" onclick="togglePasswordVisibility()">
                    Show
                </span>
            </div>
            <input type="submit" value="Sign In">
        </form>
     <!--  <p class="message">Forgot your password? <a href="/resetpassword">Reset Password  </a></p>
        <p class="message"> <a href="/sinup">SignUp Now </a></p>-->
        <div class="message-container">
            <a id="reset" href="/resetpassword"> reset password?</a>
            <a href="/sinup">Sign Up Now</a>
        </div>
        <div class="social-login-buttons">
            <a href="your_google_auth_endpoint"><i class="fab fa-google"></i></a>
            <a href="your_facebook_auth_endpoint"><i class="fab fa-facebook"></i></a>
            <a href="your_github_auth_endpoint"><i class="fab fa-github"></i></a>
        </div>
    </div>
   
    </div>

    <script>
        function togglePasswordVisibility() {
            var passwordInput = document.getElementById("password");
            if (passwordInput.type === "password") {
                passwordInput.type = "text";
                document.querySelector('.password-toggle').textContent = "Hide";
            } else {
                passwordInput.type = "password";
                document.querySelector('.password-toggle').textContent = "Show";
            }
        }
    </script>
</body>
</html>
